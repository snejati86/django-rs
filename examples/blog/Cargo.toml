[package]
name = "blog-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
django-rs = { path = "../../django-rs", default-features = true }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
http = "1"
